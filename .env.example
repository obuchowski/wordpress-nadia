# WordPress container reads these via env_file in podman-compose
WORDPRESS_DB_HOST=wordpress-mysql:3306
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-me

# MySQL container
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress
MYSQL_PASSWORD=change-me

# Simple Deploy plugin - static output path (container path)
SIMPLE_DEPLOY_STATIC_DIR=/var/www/html/static-output

# Cloudflare Pages Direct Upload
# Get Account ID from: https://dash.cloudflare.com/ → click account → URL shows account ID
# Create API Token at: https://dash.cloudflare.com/profile/api-tokens → Create Token → "Edit Cloudflare Pages"
# Project name: the name of your Pages project in Cloudflare
CLOUDFLARE_ACCOUNT_ID=your-account-id-here
CLOUDFLARE_API_TOKEN=your-api-token-here
CLOUDFLARE_PAGES_PROJECT=your-project-name
